#centroide <- colMeans(Y)
plot(Y[,2],type="n",xlab="Componente 1",ylab="Componente 2")
#arrows(,,)
points(Y[,1]-centroide[1],Y[,2]-centroide[2])
par(mar=c(4.1,4.1,2,1))
xc <- scale(x,scale=F)
Y <- xc%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
# O salto para a segunda componente é significativo, mas em diante não parece fazer diferença
# Faremos com duas componentes:
#centroide <- colMeans(Y)
plot(Y[,2],type="n",xlab="Componente 1",ylab="Componente 2")
#arrows(,,)
points(Y[,1],Y[,2])
par(mar=c(4.1,4.1,2,1))
xc <- scale(x,scale=F)
Y <- xc%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
# O salto para a segunda componente é significativo, mas em diante não parece fazer diferença
# Faremos com duas componentes:
#centroide <- colMeans(Y)
plot(Y[,1:2],type="n",xlab="Componente 1",ylab="Componente 2")
#arrows(,,)
points(Y[,1],Y[,2])
par(mar=c(4.1,4.1,2,1))
xc <- scale(x,scale=F)
Y <- xc%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
# O salto para a segunda componente é significativo, mas em diante não parece fazer diferença
# Faremos com duas componentes:
#centroide <- colMeans(Y)
plot(Y[,1:2],type="n",xlab="Componente 1",ylab="Componente 2")
arrows(0,0,E[,1],E[,2])
points(Y[,1],Y[,2])
par(mar=c(4.1,4.1,2,1))
xc <- scale(x,scale=F)
Y <- xc%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
# O salto para a segunda componente é significativo, mas em diante não parece fazer diferença
# Faremos com duas componentes:
#centroide <- colMeans(Y)
plot(Y[,1:2],type="n",xlab="Componente 1",ylab="Componente 2")
arrows(0,0,E[,1],E[,2],length=.1)
points(Y[,1],Y[,2])
E[,1]
plot(E[,1],E[,2])
plot(E[,1],E[,2])
plot(E[,1],E[,2])
arrows(0,0,E[,1],E[,2],length=.1)
E[5,]
E
par(mar=c(4.1,4.1,2,1))
#dados centrados
xc <- scale(x,scale=F)
x <- as.matrix(read.table("solo.txt",header=T,dec=",",row.names = 1))
S <- cov(x)
S.eig <- eigen(S)
lambda <- S.eig$values
lambda #lambda 5 é zero, muito pequeno.
(E <- S.eig$vectors)
#Componentes principais
Y <- x%*%E
S <- cov(x[,-3])
S.eig <- eigen(S)
lambda <- S.eig$values
lambda
S <- cov(x)
S.eig <- eigen(S)
lambda <- S.eig$values
lambda[5] <- 1E-8
(S.nova <- E%*%diag(lambda)%*%t(E))
par(mar=c(4.1,4.1,2,1))
#dados centrados
xc <- scale(x,scale=F)
#escores centrados
Y <- xc%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
# O salto para a segunda componente é significativo, mas em diante não parece fazer diferença
perc <- lambda/sum(lambda)*100
tabela.val <- cbind(round(lambda,3),round(perc,2),round(cumsum(perc),1))
rownames(tabela.val) <- paste0("Comp",1:5)
colnames(tabela.val)  <- c("Autovalor","% Variância","% Acumulada")
tabela.val
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio #no quadro: R
#quantos % da variável está em cada componente?
cor.YX^2*100
# Faremos com as duas primeiras componentes:
plot(Y[,1:2],type="n",xlab="Componente 1",ylab="Componente 2")
arrows(0,0,E[,1],E[,2],length=.05,col="red")
points(Y[,1],Y[,2])
par(mar=c(4.1,4.1,2,1))
#dados centrados
xc <- scale(x,scale=F)
#escores centrados
Y <- xc%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
# O salto para a segunda componente é significativo, mas em diante não parece fazer diferença
perc <- lambda/sum(lambda)*100
tabela.val <- cbind(round(lambda,3),round(perc,2),round(cumsum(perc),1))
rownames(tabela.val) <- paste0("Comp",1:5)
colnames(tabela.val)  <- c("Autovalor","% Variância","% Acumulada")
tabela.val
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio #no quadro: R
#quantos % da variável está em cada componente?
cor.YX^2*100
screeplot
methods("screeplot")
screeplot.default
?methods
screeplot.default
screeplot.default*
?screeplot.default
stats:::screeplot.default
?match.arg
substitute(E)
substitute(Y)
?invisible
par(mar=c(4.1,4.1,2,1))
#dados centrados
xc <- scale(x,scale=F)
#escores centrados
Y <- xc%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
# O salto para a segunda componente é significativo, mas em diante não parece fazer diferença
perc <- lambda/sum(lambda)*100
tabela.val <- cbind(round(lambda,3),round(perc,2),round(cumsum(perc),1))
rownames(tabela.val) <- paste0("Comp",1:5)
colnames(tabela.val)  <- c("Autovalor","% Variância","% Acumulada")
tabela.val
par(mar=c(4.1,4.1,2,1))
#dados centrados
xc <- scale(x,scale=F)
#escores centrados
Y <- xc%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
perc <- lambda/sum(lambda)*100
tabela.val <- cbind(round(lambda,3),round(perc,2),round(cumsum(perc),1))
rownames(tabela.val) <- paste0("Comp",1:5)
colnames(tabela.val)  <- c("Autovalor","% Variância","% Acumulada")
tabela.val
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio #no quadro: R
#quantos % da variável está em cada componente?
cor.YX^2*100
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
cor.YX^2*100
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,7)*100
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
var(x)
xs <- scale(x)
rm(xs)
x <- scale(x)
S <- cov(X)
S <- cov(x)
eigen(S)
E
x <- as.matrix(read.table("solo.txt",header=T,dec=",",row.names = 1))
S <- cov(x)
S.eig <- eigen(S)
lambda <- S.eig$values
lambda #lambda 5 é zero, muito pequeno.
(E <- S.eig$vectors)
#Componentes principais
Y <- x%*%E
S <- cov(x[,-3])
S.eig <- eigen(S)
lambda <- S.eig$values
lambda
S <- cov(x)
S.eig <- eigen(S)
lambda <- S.eig$values
lambda[5] <- 1E-8
(S.nova <- E%*%diag(lambda)%*%t(E))
par(mar=c(4.1,4.1,2,1))
#dados centrados
xp <- scale(x)
#escores centrados
Y <- xp%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
perc <- lambda/sum(lambda)*100
tabela.val <- cbind(round(lambda,3),round(perc,2),round(cumsum(perc),1))
rownames(tabela.val) <- paste0("Comp",1:5)
colnames(tabela.val)  <- c("Autovalor","% Variância","% Acumulada")
tabela.val
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
x
E
## Graficamente
biplot(E[,1:2])
## Graficamente
biplot(Y,E[,1:2])
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(x))
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(x),xlab="Comp1(96.2%)",ylab="Comp2(3.53%)")
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(x),xlab="Y1(96.2%)",ylab=expression(Y[2](3.53%)))
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(x),xlab="Y1(96.2%)",ylab=expression(Y[2](3.53"%")))
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(x),xlab="Y1(96.2%)",ylab=expression(Y[2](3.53*%)))
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(x),xlab="Y1(96.2%)",ylab=expression(Y[2](3.53*'%')))
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(x),
xlab=expression(Y[1](96.2*'%')),ylab=expression(Y[2](3.5*'%')))
biplot(Y,E[,3:4],ylabs=colnames(x),
xlab=expression(Y[3](0.20*'%')),ylab=expression(Y[4](0.11*'%')))
biplot(Y,E[4:5,3:4],ylabs=colnames(x),
xlab=expression(Y[3](0.20*'%')),ylab=expression(Y[4](0.11*'%')))
biplot(Y,E[4:5,3:4],ylabs=colnames(x)[4:5],
xlab=expression(Y[3](0.20*'%')),ylab=expression(Y[4](0.11*'%')))
x
L<- E%*%Lambda.meio #matriz de loadings
L
plot(L[,1:2])
plot(L[,1:2],pch=colnames(x))
plot(L[,1:2],type='n')
text(L[,1:2],labels=colnames(x))
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2]%*%Lambda.meio[,1:2],ylabs=colnames(x),
xlab=expression(Y[1](96.2*'%')),ylab=expression(Y[2](3.5*'%')))
## Graficamente
biplot(Y,(E%*%Lambda.meio)[,1:2],ylabs=colnames(x),
xlab=expression(Y[1](96.2*'%')),ylab=expression(Y[2](3.5*'%')))
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(x),
xlab=expression(Y[1](96.2*'%')),ylab=expression(Y[2](3.5*'%')))
## Graficamente
biplot(Y,(E%*%Lambda.meio)[,1:2],ylabs=colnames(x),
xlab=expression(Y[1](96.2*'%')),ylab=expression(Y[2](3.5*'%')))
E[,4]
pc <- prcomp(x,scale=F)
View(pc)
pc <- prcomp(x,scale.=F)
View(pc)
pc$sdev**2
pc <- prcomp(x,scale.=F)
#autovalores
rbind(lambda,pc$sdev**2)|> round(5)
pc <- prcomp(x,scale.=F)
#autovalores
rbind(lambda,pc$sdev**2)|> round(9)
pc
E
pc$center
pc$x
y
Y
par(mar=c(4.1,4.1,2,1))
#dados centrados
xc <- scale(x,scale=F)
#escores centrados
Y <- xc%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
perc <- lambda/sum(lambda)*100
tabela.val <- cbind(round(lambda,3),round(perc,2),round(cumsum(perc),1))
rownames(tabela.val) <- paste0("Comp",1:5)
colnames(tabela.val)  <- c("Autovalor","% Variância","% Acumulada")
tabela.val
V.meioinv <- diag(1/sqrt(diag(S.nova)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:5)
rownames(cor.YX)  <- colnames(x)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(x),
xlab=expression(Y[1](96.2*'%')),ylab=expression(Y[2](3.5*'%')))
pc <- prcomp(x,scale.=F)
#autovalores
rbind(lambda,pc$sdev**2)|> round(9)
#autovetores
E
pc$rotation
summary(pc)
pc$sdev
barlett.test(pc)
bartlett.test(pc)
psych::cortest.bartlett(R,n=20)
install.packages("psych")
psych::cortest.bartlett(R,n=20)
psych::cortest.bartlett(S,n=20)
psych::cortest.bartlett(x,n=20)
psych::cortest.bartlett(cor(x),n=20)
var(Y)
lambda
round(var(Y),5)
R <- cor(x)
psych::cortest.bartlett(R[1,1],n=n)
R <- cor(x)
psych::cortest.bartlett(R[1:2,1:2],n=n)
R <- cor(x)
psych::cortest.bartlett(R[1:2,1:2],n=20)
R <- cor(x)
psych::cortest.bartlett(R[3:4,3:4],n=20)
R[c(2,3),c(2:3)]
R <- cor(x)
psych::cortest.bartlett(R[c(2,3),c(2:3)],n=20)
?psych::fa.parallel
psych::fa.parallel(y,fa="pc")
psych::fa.parallel(Y,fa="pc")
cov(Y)
psych::cortest.bartlett(Y,n=20)
E <- solve(S-diag(lambda[1]))
solve(S-diag(rep(lambda[1],5)))
solve(S-diag(lambda)
)
E
solve(S-diag(rep(lambda[1],5)))
solve(S-diag(rep(lambda[1],5)),b=c(0,0,0,0,0))
S
biplot(pc)
x <- as.matrix(read.table("solo.txt",header=T,dec=",",row.names = 1))
S <- cov(x)
S.eig <- eigen(S)
lambda <- S.eig$values
lambda #lambda 5 é zero, muito pequeno.
E <- S.eig$vectors
#Componentes principais
Y <- x%*%E
S <- cov(x[,-3])
S.eig <- eigen(S)
lambda <- S.eig$values
lambda
E <- S.eig$vectors
par(mar=c(4.1,4.1,2,1))
#dados centrados
xc <- scale(x,scale=F)
#escores centrados
Y <- xc[,-3]%*%E
screeplot(list(sdev=sqrt(lambda)),type='l')
perc <- lambda/sum(lambda)*100
tabela.val <- cbind(round(lambda,3),round(perc,2),round(cumsum(perc),1))
rownames(tabela.val) <- paste0("Comp",1:4)
colnames(tabela.val)  <- c("Autovalor","% Variância","% Acumulada")
tabela.val
V.meioinv <- diag(1/sqrt(diag(S)))
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
cor.YX<-V.meioinv%*%E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:4)
rownames(cor.YX)  <- colnames(x)[-3]
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(x)[-3],
xlab=expression(Y[1](98.32*'%')),ylab=expression(Y[2](1.36*'%')))
pc <- prcomp(x[,-3],scale.=F)
#autovalores
rbind(lambda,pc$sdev**2)|> round(9)
#autovetores
E
pc$rotation
#gráfico
sort(Y[,1])
R <- V.meioinv%*%S%*%V.meioinv
R.eig <- eigen(R)
lambda <- R.eig$values
E <- R.eig$vectors
lambda
par(mar=c(4.1,4.1,2,1))
screeplot(list(sdev=sqrt(lambda)),type='l')
perc <- lambda/sum(lambda)*100
tabela.val <- cbind(round(lambda,3),round(perc,2),round(cumsum(perc),1))
rownames(tabela.val) <- paste0("Comp",1:4)
colnames(tabela.val)  <- c("Autovalor","% Variância","% Acumulada")
tabela.val
##escores
xp <- scale(x[,-3])
Y <- xp%*%E
##
Lambda.meio <-diag(sqrt(lambda))
# matriz de correlacao entre Componente(i) e Variavel(k)
#V.meioinv é identidade por R ser matriz de correlacoes
cor.YX<-E%*%Lambda.meio
colnames(cor.YX) <- paste0("Comp",1:4)
rownames(cor.YX)  <- colnames(xp)
#quantos % da variável está em cada componente?
round(cor.YX^2,5)*100
## Graficamente
biplot(Y,E[,1:2],ylabs=colnames(xp),
xlab=bquote(Y[1](.(round(perc[1],2))*'%')),
ylab=bquote(Y[2](.(round(perc[2],2))*'%')))
Y[,2]
Sigma <- matrix(c(5,2,2,2),ncol=2)
S.eig <- eigen(Sigma)
(E <- S.eig$vectors)
(lambda <- S.eig$values)
lambda[1]/sum(lambda)
V.meioinv <- diag(1/sqrt(diag(S)))
(P <- V.meioinv%*%S%*%V.meioinv)
P.eig <- eigen(P)
(E <- P.eig$vectors)
(lambda <- P.eig$values)
P
lambda[1]/sum(lambda)
V.meioinv <- diag(1/sqrt(diag(S)))
(P <- V.meioinv%*%S%*%V.meioinv)
V.meioinv <- diag(1/sqrt(diag(Sigma)))
(P <- V.meioinv%*%S%*%V.meioinv)
V.meioinv <- diag(1/sqrt(diag(Sigma)))
(P <- V.meioinv%*%Sigma%*%V.meioinv)
E
P.eig <- eigen(P)
(E <- P.eig$vectors)
(lambda <- P.eig$values)
E
Lambda <- diag(S.eig$values)
Gama2 <- diag(P.eig$values)
Lambda%*%Gama2
